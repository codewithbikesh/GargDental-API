<div class="form-check form-switch">
    <input class="form-check-input single-switch" 
           type="checkbox" 
           data-id="{{ $row->id }}" 
           {{ $row->status ? 'checked' : '' }}>
</div>
